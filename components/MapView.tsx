'use client';
import {MapContainer,TileLayer,Marker,Popup,Polyline} from 'react-leaflet'; import L from 'leaflet'; import 'leaflet/dist/leaflet.css';
const icon=(emoji:string)=>L.divIcon({html:`<div class="pin">${emoji}</div>`,className:'',iconSize:[36,36],iconAnchor:[18,18]});
export default function MapView({origin,destination,geometry}:{origin:any;destination:any;geometry:[number,number][]}){const center:[number,number]=[(origin.lat+destination.lat)/2,(origin.lon+destination.lon)/2]; const positions=geometry.map(([lon,lat])=>[lat,lon] as [number,number]); return <MapContainer center={center} zoom={6} scrollWheelZoom={true} className="map"><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><Polyline positions={positions}/><Marker position={[origin.lat,origin.lon]} icon={icon('A')}><Popup>{origin.name}</Popup></Marker><Marker position={[destination.lat,destination.lon]} icon={icon('B')}><Popup>{destination.name}</Popup></Marker></MapContainer>}
